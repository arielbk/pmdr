{
  "name": "@arielbk/pmdr",
  "version": "0.3.0",
  "type": "module",
  "license": "MIT",
  "bin": {
    "pmdr": "dist/index.js"
  },
  "files": [
    "dist",
    "bundled-app"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup",
    "build:app": "../../scripts/build-menubar-zip.sh",
    "dev": "tsup --watch",
    "lint": "eslint src",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "citty": "^0.2.2"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "tsup": "^8.0.0",
    "typescript": "5.9.2",
    "vitest": "^2.0.0"
  }
}
